/**
 * Paid Bundles / Creem checkout.
 * Off until the merchant/shop application is approved — free library only.
 */
export const SHOP_ENABLED = false;

export function isShopEnabled(): boolean {
  return SHOP_ENABLED;
}
